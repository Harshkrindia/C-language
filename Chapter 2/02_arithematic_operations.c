#include <stdio.h>

int main(){
    int a = 5;
    int b = 3;
    int c = a + b;

    printf("The value of a is %d and of b is %d and their sum is %d\n", a, b, c);
    // % is modulus operator which returns the remainder when divided.
    printf("The the remainder when a is divided by b is %d", a%b);
    // a^b is not exponential function.

    return 0;
}

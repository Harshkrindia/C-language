#include<stdio.h>

int main(){
    // int a;
    // a = 1;
    int a = 1;

    // float b = 1.4;
    float b;
    b = 1.3;

    // char c = 'a';
    // it should be in single interved comma
    char c;
    c = 'a';
// you can bring multiple curors by pressing alt and then clicking. Its a feature of VS code
    printf("The value of the given a is %d\n", a);
    printf("The value of the given b is %f\n", b);
    printf("The value of the given c is %c\n", c);

}
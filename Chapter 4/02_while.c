#include <stdio.h>

int main(){
    int i = 0;
    while(i<4){
        printf("Happy birthday\n");
        // i = i+1;
        i++; // This also does the same function as above one did. It increases the value by 1.
    }
    return 0;
}